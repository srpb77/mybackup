const add = (a,b)=>{

return a+b;

}

const sub = (a,b)=>{

    return a-b;
    
    }

    const multiply = (a,b)=>{

        return a*b;
        
        }

const name = 'santosh';

module.exports={add,sub,multiply,name};
